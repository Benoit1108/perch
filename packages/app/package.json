{
  "name": "@perch/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Le corps : rendu Electron, overlay, adaptateurs de capteurs.",
  "main": "./dist/main/index.js",
  "scripts": {
    "assets": "node ./scripts/copy-renderer.mjs",
    "start": "npm run assets && node ./scripts/start.mjs"
  },
  "dependencies": {
    "@perch/core": "*",
    "zod": "^4.4.3",
    "dbus-next": "^0.10.2"
  },
  "devDependencies": {
    "electron": "^42.7.0"
  }
}
